# HERMES experiments — sensitivity variant: epsilon_bit × 1.5
#
# Derived from experiments/calibration.toml (verified). Only epsilon_bit
# is perturbed; all other constants and provenance unchanged.

[exp1.aerpaw_usrp]
P_idle_W = 5.0
epsilon_bit_J_per_bit = 1.05e-9
B_nominal_bps = 10_000_000

[exp3.mule_platform]
P_idle_W = 5.0
epsilon_bit_J_per_bit = 1.05e-9
epsilon_prop_J_per_m = 10.0
mule_cruise_speed_m_s = 5.0

[provenance]
last_verified = "2026-05-05"
source = "Sensitivity variant of verified calibration: epsilon_bit scaled to 1.5x baseline (7.0e-10 -> 1.05e-9 J/bit). All other values unchanged. See experiments/calibration.toml for primary provenance."
status = "verified"

# Per-constant status (see experiments/calibration.toml for the rationale).
# This variant sweeps an Experiment 1 constant; it carries the same
# un-replaced propulsion coefficient as the primary calibration, so that
# constant stays marked placeholder here too. Experiment 1 figures are
# unaffected because epsilon_prop is not an Experiment 1 constant.
[provenance.status_by_constant]
"exp3.epsilon_prop_J_per_m" = "placeholder"
