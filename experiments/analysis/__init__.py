"""Post-trial analysis for HERMES experiments.

Each experiment has its own module: :mod:`experiments.analysis.exp1`
for Federated-vs-Centralized, ``experiments.analysis.exp3`` (deferred
until Experiment 3 produces CSV data).

The modules are deliberately written as plain Python rather than
Jupyter notebooks — easier to test, easier to diff, easier to import
into an actual notebook for interactive exploration.
"""
