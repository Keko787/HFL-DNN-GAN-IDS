[pytest]
# L-L7: register custom markers so pytest doesn't emit
# PytestUnknownMarkWarning when tests use @pytest.mark.slow.
#
# Run only the fast suite:
#     pytest -m "not slow"
# Run just the slow integration tests:
#     pytest -m slow
markers =
    slow: integration tests that spawn real subprocesses (~3-5s each)
